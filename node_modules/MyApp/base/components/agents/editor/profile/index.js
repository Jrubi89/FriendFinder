import SocialMedia from 'components/users/social-media'
import Notes from 'components/users/notes'
import Headline from './headline'
import ResendWelcomeEmail from './resend-welcome-email'

export default {
  props: ['item'],
  template: require('./template'),

  components: {
    Headline,
    SocialMedia,
    ResendWelcomeEmail,
    Notes
  },

  methods: {
    normalize_website_url(url) {return url.match(/^\w+\:\/\//) ? url : `http://${url}`}
  }
}
