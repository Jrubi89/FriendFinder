import Status from '../status'

import member_status_map from '../../../member_status_map'

export default {
  props: ['item'],
  template: require('./template'),

  data() {return {
    member_status_map
  }},

  components: {
    Status,
  }
}
