import Export from './export'
import Download from './download'

export default {
  template: require('./template'),

  computed: {
    active_component() {return this.item.exported_at ? 'Download' : 'Export'}
  },

  components: {
    Export,
    Download
  }
}
