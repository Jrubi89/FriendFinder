export default {
  props: ['item'],
  template: require('./template'),

  methods: {
    download() {
      let url = this.env.components.TaxExports.url('download', this.item.id)
      this.$el.querySelector('iframe').setAttribute('src', url)
    }
  }
}
