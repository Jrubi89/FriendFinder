import Item from './item'
import Editor from './editor'

export default {
  components: {
    Item,
    Editor,
    ListHeader: {
      created() {this.$store.commit('CRUDLE__CREATE_UI', false)}
    }
  }
}
