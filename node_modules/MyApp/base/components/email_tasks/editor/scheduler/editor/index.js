import Editor from 'helpers/medium-editor'

export default {
  props: ['email'],
  template: require('./template'),

  mounted() {
    Editor(
      this.$el.querySelector('[name="body"]'),
      this.$el.querySelector('#toolbar')
    )
  }
}
