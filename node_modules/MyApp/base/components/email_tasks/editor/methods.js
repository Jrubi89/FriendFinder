import {scroll_top} from 'appril-utils'

export function filter_emails() {
  this.api.emails(this.task.id, this.$store.state.EmailTasks.filters).send({
    success: (emails) => {
      this.$store.commit('EMAIL_TASKS__SET_EMAILS', emails)
      scroll_top()
    }
  })
}
