import Create from './create'
import Receipt from './receipt'

export default {
  template: require('./template'),

  created() {
    this.$store.commit('CRUDLE__UPDATE_UI', false)
  },

  components: {
    Create,
    Receipt
  },

  computed: {
    current_view() {return this.item.draft ? 'Create' : 'Receipt'}
  }
}
