import AddressEditor from './address-editor'
import * as computed from '../computed'

export default {
  props: ['user'],
  template: require('./template'),
  computed,

  components: {
    AddressEditor
  },

  methods: {
    open_address_editor() {this.$store.commit('ORDERS__EDIT_ADDRESS', true)}
  }
}
