import * as computed from '../computed'

export default {
  props: ['product'],
  template: require('./template'),

  computed,

  methods: {
    set_size(evt) {
      this.$store.commit('ORDERS__SET_SIZE', {
        product: this.product,
        size: evt.target.options[evt.target.selectedIndex].value
      })
    }
  }
}
