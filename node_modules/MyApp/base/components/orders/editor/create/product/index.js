import Colors from './colors'
import Sizes from './sizes'

import * as methods from './methods'
import * as computed from './computed'

export default {
  template: require('./template'),

  components: {
    Colors,
    Sizes
  },

  computed: {
    ...computed,
    product(state) {
      return this.$store.state.Orders.current_product
    }
  },
  
  methods
}
