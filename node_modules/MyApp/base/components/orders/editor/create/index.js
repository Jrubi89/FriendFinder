import Products from './products'
import Product from './product'
import User from './user'
import Cart from './cart'
import {cart} from './cart/computed'
import {edit_address} from './user/computed'

export default {
  template: require('./template'),

  components: {
    Products,
    Product,
    User,
    Cart
  },

  computed: {
    cart,
    edit_address,
    product_view() {
      return this.$store.state.Orders.current_product ? 'Product' : 'Products'
    }
  }
}
