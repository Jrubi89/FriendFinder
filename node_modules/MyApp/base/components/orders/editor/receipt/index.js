import Receipt from '../../receipt'
import MainControls from './main-controls'
import ShipmentControls from './shipment-controls'

export default {
  props: ['order'],
  template: require('./template'),

  components: {
    MainControls,
    ShipmentControls,
    Receipt
  },

  computed: {
    controls_view() {
      return this.$store.state.Orders.shipment_controls ? 'ShipmentControls' : 'MainControls'
    }
  }
}
