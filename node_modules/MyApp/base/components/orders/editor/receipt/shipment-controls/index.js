import {update_tracking_number, close_shipment_controls} from '../methods'

export default {
  props: ['order'],
  template: require('./template'),

  methods: {
    update_tracking_number,
    close_shipment_controls
  }
}
