import {delete_sku, edit_sku, sku_stock} from '../../methods'

export default {

  props: ['sku'],

  template: require('./template'),

  methods: {
    delete_sku, 
    edit_sku, 
    sku_stock
  }
}
