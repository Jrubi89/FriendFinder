import Skus from './skus'
import Edit from './edit'
import Stock from './stock'

export default {
  props: ['item'],
  template: require('./template'),

  components: {
    Skus,
    Edit,
    Stock
  },

  computed: {
    inventory_view() {return this.$store.state.Products.inventory_view}
  }
}
