import {save_sku, close} from '../methods'

export default {
  template: require('./template'),

  computed: {
    sku() {return this.$store.state.Products.inventory_sku}
  },

  methods: {
    save_sku,
    close
  }
}
