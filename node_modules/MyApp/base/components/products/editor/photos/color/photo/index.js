import {delete_photo} from '../../methods'

export default {
  props: ['photo', 'color'],
  template: require('./template'),

  computed: {
    src() {return `${this.env.product_photos_url}/${this.photo.original}`}
  },

  methods: {
    delete_photo
  }
}
