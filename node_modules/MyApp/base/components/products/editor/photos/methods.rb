class Products

  def upload_photo product_id, params
    @product.upload_photo(params)
  end

  def delete_photo product_id, params
    @product.delete_photo(params)
  end

  def update_photo_label product_id, params
    @product.update_photo_label(params[:original], params[:label])
  end

end
