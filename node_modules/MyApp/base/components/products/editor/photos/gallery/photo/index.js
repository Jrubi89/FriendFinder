import {delete_photo, update_label} from '../../methods'

export default {
  props: ['photo'],
  template: require('./template'),

  computed: {
    src() {return `${this.env.product_photos_url}/${this.photo.original}`}
  },

  methods: {
    delete_photo,
    update_label
  }
}
