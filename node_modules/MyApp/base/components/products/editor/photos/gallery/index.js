import Photo from './photo'
import {initiate_upload, upload} from '../methods'

export default {
  props: ['item'],
  template: require('./template'),

  components: {
    Photo
  },

  methods: {
    initiate_upload, 
    upload
  }
}
