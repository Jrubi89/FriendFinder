
export default {
  props: ['credit'],

  template: require('./template'),

  data() {
    return {
      consumption_order_url: (consumption) => {
        return this.$options.stores[consumption.store].url + '/orders?order_id=' + consumption.order_id
      }
    }
  }
}
