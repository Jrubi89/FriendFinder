import CodeMirror from 'appril-codemirror'

export default {
  props: ['item'],
  template: require('./template'),

  data() {return {
    meta_robots: [
      'index, follow',
      'index, nofollow',
      'noindex, follow',
      'noindex, nofollow',
    ]
  }},

  mounted() {
    CodeMirror(this.$el.querySelector('[name="content"]'))
  }
}
