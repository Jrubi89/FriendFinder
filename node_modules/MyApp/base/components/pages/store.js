export default {
  state: {},
  
  mutations: {

    PAGES__ADD_ALIAS({item}, alias) {
      item.aliases.unshift(alias)
    },

    PAGES__DELETE_ALIAS({item}, index) {
      item.aliases.splice(index, 1)
    }
  }
}
