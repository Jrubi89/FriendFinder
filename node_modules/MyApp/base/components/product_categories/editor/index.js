import CodeMirror from 'appril-codemirror'

export default {
  template: require('./template'),

  mounted() {
    let cm = CodeMirror(this.$el.querySelector('[name="layout"]'))
    cm.setValue(this.item.layout || '')
    cm.save()
  }
}
