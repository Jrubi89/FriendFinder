import Alert from 'appril-alert'

import RequestUpdates from '../request-updates'
import Approve from '../approve'

export default {
  props: ['profile'],
  template: require('./template'),

  components: {
    RequestUpdates,
    Approve,
  }
}
