import {map} from 'lodash'

export function render_gallery(index) {
  $.magnificPopup.open({
    items: map([this.profile.photo].concat(this.profile.photos), (p) => ({src: p && p.original_url})),
    type: 'image',
    gallery: {enabled: true},
  }, index)
}
