export default {
  props: ['profile', 'spec'],
  template: require('./template'),

  computed: {
    specObject() {return this.$options.specs[this.spec]},
    value() {return this.specObject && this.profile[this.spec] && this.specObject.opts[this.profile[this.spec]]}
  }
}
