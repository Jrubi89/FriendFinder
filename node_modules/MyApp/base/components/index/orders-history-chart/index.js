import c3 from 'c3'

export default {
  props: ['data'],
  template: require('./template'),

  mounted() {
    this.$nextTick(() => {
      c3.generate({
        bindto: this.$el.querySelector('#chart'),
        data: {
          columns: [
            ['Approved'].concat(this.data.approved),
            ['Declined'].concat(this.data.declined),
            ['Voided'].concat(this.data.voided),
            ['Refunded'].concat(this.data.refunded),
          ]
        },
        axis: {
          x: {
            type: 'category',
            categories: this.data.months
          },
          y: { show: false }
        }
      })
    })
  }
}
