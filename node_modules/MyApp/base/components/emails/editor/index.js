import CodeMirror from 'appril-codemirror'

export default {
  template: require('./template'),

  mounted() {
    let cm = CodeMirror(this.$el.querySelector('[name="body"]'))
    cm.setValue(this.item.body || '')
    cm.save()
  }
}
