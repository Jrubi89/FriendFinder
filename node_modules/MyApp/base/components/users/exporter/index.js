import Serialize from 'form-serialize'

export default {
  template: require('./template'),

  methods: {
    trigger_export(evt) {
      let url = this.component.url('export', Serialize(evt.target.closest('form'), {hash: true}))
      this.$el.querySelector('iframe#export').setAttribute('src', url)
    }
  }
}
