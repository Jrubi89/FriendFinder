
%w[
  base_controller.rb
  rtcp_controller.rb
  **/server.rb
  **/*.rb
].each do |p|
  Dir[File.expand_path("../../base/components/#{p}", __FILE__)].sort {|a,b|
    a.split(/\/+/).size <=> b.split(/\/+/).size
  }.each {|f| require(f)}
end
